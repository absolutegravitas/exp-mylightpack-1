import React from "react"

const HeroDescription = () => {
  return (
    <p className="text-lg text-gray-600 dark:text-gray-400">
      myLightPack helps you count every gram, because when every gram counts, optimizing your pack weight can make all
      the difference on your outdoor adventures.
    </p>
  )
}

export default HeroDescription
