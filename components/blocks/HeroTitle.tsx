import React from "react"

const HeroTitle = () => {
  return (
    <h1 className="text-4xl font-bold text-gray-900 md:text-5xl dark:text-gray-100">
      Count every gram, when every gram counts.
    </h1>
  )
}

export default HeroTitle
